from django.apps import AppConfig


class ServicesConfig(AppConfig):
    name = 'services'
    verbose_name = 'Gestor de servicios' #Asignarle un nombre público al app, se añade una configuración extendida
